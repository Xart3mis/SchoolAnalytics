# Completion checklist

- Run `npm run lint` if relevant UI/code changes were made.
- Run `npm run prisma:generate` or `npm run prisma:migrate` when schema changes.
- Note: no tests/formatters defined in package.json.
